ATHENA_DATABASE   = my_s3_inventory
OUTPUT_BUCKET     = my-athena-output-bucket
OUTPUT_PREFIX     = athena-results/
REPORT_PREFIX     = reports/
SENDER_EMAIL      = no-reply@example.com    # verified in SES (or use SES role)
RECIPIENT_EMAIL   = ops-team@example.com     # destination (must be verified if SES in sandbox)
SES_ROLE_ARN      =                            # optional: role to assume for SES
AWS_REGION        = us-east-1
TOP_N             = 15

